{
  "name": "@e2e-tests",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "lint:fix:unsafe": "biome check --write --unsafe",
    "type-check": "tsc --noEmit",
    "test:e2e": "pnpm exec playwright test",
    "test:e2e:ui": "pnpm exec playwright test --ui",
    "test:e2e:report": "pnpm exec playwright show-report",
    "check:ci": "pnpm type-check && pnpm knip && biome ci",
    "check:commit": "pnpm type-check && pnpm knip && biome ci",
    "knip": "knip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@shared": "workspace:*",
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.2.1",
    "knip": "^5.83.0"
  }
}
