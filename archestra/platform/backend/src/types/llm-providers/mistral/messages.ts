/**
 * Mistral message schemas - OpenAI-compatible
 *
 * Mistral uses an OpenAI-compatible API, so we re-export OpenAI schemas.
 * @see https://docs.mistral.ai/api
 */
export { MessageParamSchema, ToolCallSchema } from "../openai/messages";
